import {Dispatch, SetStateAction} from "react";

function Filter({state, setFilters} : {state: NonNullable<unknown>, setFilters: Dispatch<SetStateAction<never[]>>}) {
	
	return(
		<>
			<div id={"filter"}>
			
			</div>
		</>
	);
}

export default Filter;